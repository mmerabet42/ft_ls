#include "ft_lst.h"
#include "libft.h"

int	main(int argc, char **argv)
{
	t_lsops	lsops;

	lsops = ls_get_options(argc, argv);

	/*
	while ((d = readdir(dird)))
	{
		ft_printf("Type : %c\n", d->d_type);
		ft_printf("Directory name : %{cyan}%s%{0}\n", d->d_name);
	}
*/
	return (0);
}
